[package]
name = "types"
version = "0.1.0"
edition = "2024"

[dependencies]
log = "0.4"
tokio = { version = "1", features = ["sync"] }
chrono = { version = "0.4", default-features = false, features = [
    "clock",
    "std",
] }
