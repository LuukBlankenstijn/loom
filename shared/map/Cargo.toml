[package]
name = "loom-map"
version = "0.1.0"
edition = "2024"

[dependencies]
iced = { version = "0.14.0", features = ["canvas", 'web-colors', 'webgl'] }
uuid = { version = "1.20.0", features = ["v7", "js"] }
ordermap = "1.1.0"
loom-map-types = { path = "./types"}
